# ---------------------------------------------------------
#	text_read.R
#
#				Mar/07/2018
#
# ---------------------------------------------------------
write("*** 開始 ***", stderr())
file_in <- commandArgs(trailingOnly=TRUE)[2]
print (file_in)
read.table (file_in)
write("*** 終了 ***", stderr())
# ---------------------------------------------------------
