#! /usr/bin/gosh
;; -*- coding: utf-8 -*-
;; ----------------------------------------------------------------------
;;	mcachedb_delete.scm
;;
;;					Oct/04/2010
;;
;; ----------------------------------------------------------------------
(use memcache)

;; ----------------------------------------------------------------------
(define (main args)
(define id_in (car (cdr args)))

(define conn (memcache-connect "localhost" 21201))
(print "*** 開始 ***")
(print id_in)
(print (delete conn id_in))

(print "*** 終了 ***")

0
)
;; ----------------------------------------------------------------------
